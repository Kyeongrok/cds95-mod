# Local test install (actual game exe/save data, not part of this mod's source)
/cds95runfile/

# Browser automation snapshots/cache
/.playwright-mcp/

# Plugin dev build output
/plugins-src/build/
/plugins-src/out/

# Third-party build dependency (cloned locally per plugins-src/README.md; CI release uses prebuilt binaries)
/plugins-src/third_party/

# JetBrains IDE
/.idea/
